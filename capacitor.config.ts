import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.manglist',
  appName: 'MangList-Ionic',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
