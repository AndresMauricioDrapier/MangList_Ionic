import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.manglist_ionic',
  appName: 'manglist_ionic',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
